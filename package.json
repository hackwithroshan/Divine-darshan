{
  "name": "astrologica-frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "esbuild index.tsx --bundle --outfile=index.js --servedir=. --serve=:3000",
    "build": "node build.js",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "esbuild": "latest",
    "fs-extra": "^11.2.0",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "recharts": "latest"
  },
  "devDependencies": {
    "@types/react": "^18.2.0"
  }
}